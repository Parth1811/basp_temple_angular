type FestivalEvent @model
  @auth (
    rules: [
      { allow: private },
      { allow: public, provider: iam, operations: [read] }
    ]
  ){
  id: ID!
  title: String!
  date: String!
  description: String
  img_link: String
}
